extends layout

block content
    div(id="latlon" class="makemegopoof") !{latlon}
    script.
        window.onload = function(){
            var latlon = document.getElementById('latlon').textContent;
            displayLatLon(latlon);
        }
    nav.navbar.fixed-top.bg-white
        a.navbar-brand(style="color: #0049ff;") !{latlon}

    div(id="map" class="map")

    nav.navbar.fixed-bottom.bg-white
        #nav-content
            ul.nav
                li.nav-item
                    a.nav-link(onclick="displayAll()") All
                li.nav-item
                    a.nav-link(onclick="retrieveLocations('mastersheet', true)") Winkels
                li.nav-item
                    a.nav-link(onclick="retrieveLocations('restsheet', true)") Restaur
                li.nav-item
                    a.nav-link(onclick="retrieveLocations('culturesheet', true)") Cultuur
